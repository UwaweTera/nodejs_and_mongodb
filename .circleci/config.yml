version: 2
jobs:
  build:
    docker:
      - image: circleci/node:18.12.1
    steps:
      - checkout
      - run: 
          name: Install dependencies
          commands: |
            npm install
      - run:
          name: Run tests
          commands: |
            npm test