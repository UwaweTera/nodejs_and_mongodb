version: 2
jobs:
  build:
    docker:
      - image: circleci/node@5.0.2
    steps:
      - checkout
      - run: npm install
      - run: npm test
      - run: nyc report --reporter=text-lcov | coveralls